<div class="cover-user-edit">
  <div class="container">
    <div class="row justify-content-center">
      <h1 class="font-weight-normal mt-5 mb-5">プロフィール編集</h1>
    </div>
    <div class="row justify-content-center">
      <%= form_with model: @user, local:true do |f| %>
      <div class="form-group">
        <label class="form-label">ユーザーネーム/企業・ブランド名</label>
        <%= f.text_field :user_name, autofocus: true, id:"inputName", placeholder:"ユーザーネーム", class: "form-control" %>
      </div>
      <div class="form-group">
        <label class="form-label">プロフィール画像</label><br>
        <%= f.file_field :image %>
      </div>
      <div class="form-group">
        <label class="form-label">メールアドレス</label>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %><br>
      </div>
      <div class="form-group">
        <label class="form-label">パスワード</label><br>
        <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "new-password" %>
      </div>
      <div class="form-group">
        <label class="form-label">パスワード確認用</label>
        <%= f.password_field :password_confirmation, autocomplete: "new-password" %><br>
      </div>
      <div class="form-group">
        <label class="form-label">電話番号</label>
        <%= f.text_field :telephone_number, autofocus: true, class: "form-control" %><br>
      </div>
      <%= f.submit '保存する', class: "btn btn-success" %>
      <% end %>
    </div>
  </div>
</div>